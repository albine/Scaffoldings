### Maven Archetype for maven archetyping

A minimal archetype for customized maven project scaffolding

* Usage
the `name` attribute of `<archetype-descriptor>` should be the same as `artifactId` in the archetype `pom.xml`